import { SimplestyleDirective } from './simplestyle.directive';

describe('SimplestyleDirective', () => {
  it('should create an instance', () => {
    const directive = new SimplestyleDirective();
    expect(directive).toBeTruthy();
  });
});
